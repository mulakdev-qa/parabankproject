{
  "name": "parabankproject",
  "version": "1.0.0",
  "description": "ParaBank automation framework",
  "scripts": {
    "bddgen": "bddgen",
    "test": "bddgen && playwright test",
    "test:sign-in-page": "bddgen && playwright test --grep @sign-in",
    "test:login": "bddgen && playwright test --grep @login",
    "test:balance": "bddgen && playwright test --grep @balance",
    "generate:excel": "node scripts/generate-test-cases-excel.js"
  },
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.0.1",
    "dotenv": "^17.4.2",
    "playwright-bdd": "^9.2.0",
    "typescript": "^6.0.3",
    "xlsx": "^0.18.5"
  }
}
